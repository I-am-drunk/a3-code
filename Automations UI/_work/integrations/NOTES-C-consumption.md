# NOTES C — agent consumption / custom connections / shell

## 1. Custom connections = readiness checks, NOT agent integrations (PROVEN)
- Routes: `/_user/settings/_org/insights/custom-connections` (formatted/custom-connections-Su3LZTFL.js:17, scope kind `org`) and `/_user/settings/_org/enterprise-insights/custom-connections` (formatted/custom-connections-JzqGrFh6.js:20, scope kind `account`, enterpriseId).
- `custom-DleglJ_d.js` is a *different* thing: `/_user/settings/_org/mcp-marketplace/setup/custom` -> redirects `to: /settings/connections/custom-mcp` (line 9, 21). "Custom MCP" lives under Connections nav.
- CustomConnectionsPage-7VldPHAb.js: page def kind `connection` (line ~158), uses CustomProfileEditor (`_` = editor page, `x` = card shell, `b` = maxLength map). Card fields: `check.title` (required), `check.description` (required, textarea rows 5), `check.connect_hint` (optional, null when empty). i18n keys `readiness.customConnections*`.
- Copy (en-CsbykFSh.js): "Describe systems Devin should be able to reach, verified from Connections."; DescriptionHelp: "In plain language: the system Devin should reach and how to tell that access works. Devin verifies it read-only with your organization's secrets, MCP servers, and integrations."; Placeholder: "e.g. Call GET https://sonarqube.example.com/api/authentication/validate with the SONARQUBE_TOKEN org secret and confirm it returns valid: true"; RefreshHelp: "Runs each check read-only in a session with this organization's secrets and MCP servers."; Saved: "Saved as revision {{revision}} — applies to the next connection refresh"; Count "{{count}} of {{max}} connections".
- => A custom connection is a natural-language health check the agent runs in a session; it carries NO auth config/base URL/headers. Credentials come from org secrets / MCP servers / integrations.

## 1b. Custom connections API (PROVEN, integrations/formatted/queries-DslOFrcV.js)
- GET `${orgBase}/readiness/connections` (l.74), GET `${orgBase}/readiness/connections/checks` (l.77), POST `${orgBase}/readiness/connections/refresh` (l.78), GET `enterprises/${id}/readiness/connections` (l.87).
- GET/PUT `${orgBase}/readiness/custom-checks?kind=<repo|connection>` body `{kind, checks:[{title,description,connect_hint,kind}]}` (l.104-131); enterprise variant `enterprises/${id}/readiness/custom-checks`. Response has `revision`; 422 -> `ReadinessCustomProfileValidationError` (l.109). Max lengths: title 120, description 2000, rationale 220, connect_hint 220 (CustomProfileEditor-73QUsAfP.js:114). Cap 15 checks total across kinds (l.202: `15 - (total - thisKind)`).
- Consumers: ReadinessPage-D_9zEPlF.js, enterprise-insights-Ddu2-L8T.js (status uses `status: running|error`, `finished_at`, `error`, `connected_orgs`, `checked_orgs`, `slug`).

## 2. How automations/sessions carry integrations (PROVEN)
- integrations/formatted/prefill-iaMbQj5W.js:18-23: automation payload fields `recommended_mcps` (array of MCP keys), `slack_tool_channels`, `slack_dm_scope`, `linear_tools_enabled`, `net_policy`, `devin_mode`, `run_as_user`, `slack_reply_access`. Templates supply `required_mcps` -> `recommended_mcps` (l.45).
- NO `integration_id`, `enabled_integrations`, `available_integrations`, `integrations_enabled` anywhere in bundle (rg census). `connection_id` only appears in SCM/SSO/trigger contexts (repo connection ids), not as a session parameter.
- Per-Devin "Tool access" page `/_user/devins/$id_/tools` (integrations/formatted/_id_.tools-BGK2ngli.js:263): rows = `native:slack` (kind connected, shown if slack state connected/user-not-linked, l.164-171), `native:linear` toggle -> `linear_tools_enabled` (l.149,174), then one toggle per installed+enabled MCP server from `useAutomationMcpCatalog` -> `recommended_mcps` set (l.141-147,185-195). Filter he(): `is_installed && is_enabled && !plugin_managed/user-scoped unless run_as_user` (l.50-60). Persists via automation update mutation `{automationId, params}` (l.130-134). "Add MCP" link -> `/settings/mcp-marketplace` (l.224). Invalidates `['mcp','servers']` on visibility.
- => Model: integrations (Slack, Linear) are NATIVE tools gated by org connection + per-automation flags; everything else the agent gets is an MCP server. Git SCM integrations are not in the tools list at all (they are repo access, not tools).

## 3. MCP vs integration boundary (PROVEN)
- MCP server entry (enterprise editor save payload, formatted/EnterpriseMcpServerEditorPage-D1IaE0UK.js:893-925): name, description, short_description, icon, is_enabled, transport `HTTP|STDIO` (l.527), auth_method `none|auth_header|oauth` (l.533,653,864), auth_header_key, token_scope `org|user`, oauth_client_id/secret, oauth_grant_type `authorization_code|client_credentials` (l.543), oauth_scope, oauth_resource, headers, url (non-STDIO), command/args/env_variables (STDIO), execution_location `remote|auto` (l.580), marketplace_server_id. Routes `/settings/connections/enterprise-mcp/$installationId`.
- Declared (plugin manifest) MCP config: MCPDeclaredConfiguration-LNl7WA__.js:80-86 schema {type, transport, url, command, args[], env{}, headers{}}; transport defaults stdio if command else http (l.23).
- Installed MCP server object (useAutomationMcpCatalog-Dkg2hTpZ.js:4-30): slug, transport, is_installed, installation_id, is_enabled, auth_method, url, command, executes_on_session_machine, installation_scope, has_oauth_tokens, auth_state, oauth_refresh_invalid, has_static_credentials, marketplace_server_slug, config_key, owner_scope, plugin_managed. Catalog = org servers + enterprise `inherited`.
- MCP key used in recommended_mcps = `config_key ?? slug` (ConnectionsSection-nv--b3JI.js:70-72 Le()).
- OVERLAP: ConnectionsSection-nv--b3JI.js:986-990 hides marketplace MCP slugs `linear`, `slack`, `slack-remote` with i18n `automations.useBuiltInLinearInstead` / `useBuiltInSlackInstead`. Also `native:pylon` (l.985). So Linear and Slack exist BOTH as marketplace MCPs and as native built-in tools; UI steers to native.
- McpServiceIcons-DxhxAsNS.js is icon SVGs only, no slug map (exports letters); slug->icon map lives in app-initial-tbOtyZLY (not read).

## 4. Shell / CLI / cache
- sessions.desktop.main.js nav table l.659300-659440: `personal-connections`, `connections`, `mcp-marketplace` (href /settings/connections!), `integrations-{github,slack,microsoftTeams,bitbucket,perforce}` all href under /settings/connections, permission UseDevinSessions. Shell just deep-links to web app.
- extension.js: ACP session/new schema l.27260-27282: mcpServers: array of http|sse|acp|stdio server defs; `mcp_config.json` path helper l.118430; mcpConfigPath l.6298. Only "connections" hits are `devin.reloadAcpConnections`/`reconnectAcpConnections` (ACP transport, not integrations).
- CLI strings: `/integrations`, `/settings/integrations`, `IntegrationStatus{name,is_installed}`, `ConnectPatResponse{github_username}`, `_cognition.ai/integrations/github/connect-pat`, "Failed to check integrations, skipping git setup", "Connect GitHub as App". Tools: mcp_list_servers/mcp_list_tools/mcp_call_tool/mcp_read_resource; system prompt "Use mcp_list_servers to discover configured integrations". Full MCP OAuth client (DCR, PKCE, refresh, .well-known discovery, "MCP server is blocked by policy").
- docs mcp/configuration.mdx:118-131 fields: command,args,env,disabled | url,transport http|sse,headers,oauthClientId,oauthClientSecret,oauthResource. `devin mcp login/logout`. overview.mdx:112 Linear/Atlassian/Notion are remote OAuth MCPs.
- ~/.codeium/windsurf/mcp_config.json absent. state.vscdb copy -> only `windsurf.acp.connectorRegistryCache` (devin-cli local / devin-cloud) + acp messageStore; zero integration/oauth keys.
- T3: packages/contracts/src/providerRuntime.ts has mcp_tool_call, mcp.status.updated, mcp.oauth.completed; AcpSessionRuntime.ts passes mcpServers to session/new (l.96,736).
