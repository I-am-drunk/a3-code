# NOTES-B-oauth (task B: OAuth / connect / disconnect / token custody)

Paths: F=`_work/formatted/`, I=`_work/integrations/formatted/`, EN=`_work/formatted/en-CsbykFSh.js` (line 20017 holds the JSON `settings.*` blob)

## Callback routes (PROVEN)
- `/_user/integrations/mcp/oauth-callback` — F/oauth-callback-YK4w8hpK.js:27-90. Reads search `mcp_oauth_code`, `mcp_oauth_state` (l.28-30). POST `mcp/oauth/complete` json `{code,state}` (l.47). Response fields read: `ok`, `error`, `return_to`, `devin_id` (l.54-66). Success -> `return_to` (+`oauth_success=true`) or `/sessions/<devin_id sans devin->` or default `/customize?tab=mcps`. Error 403 -> `errorCode ?? oauth_wrong_user`; else `oauth_complete_error`; missing params -> `missing_parameters` (l.83). Error redirect appends `oauth_error=<code>`. HTTP error body parse: `detail` string or `{message,error_code}` (l.12-24).
- `/_user/integrations/git/oauth-callback` — F/oauth-callback-hJD-jXng.js:12-50. Reads `provider`, `code`, `state` (l.15). POST `integrations/git/user-oauth/${provider}/complete` json `{code,state}` (l.24-27). Success: `window.location.href = e.redirect_url` (server decides destination) (l.29). Error: 403 -> `oauth_wrong_user`, else `oauth_complete_error`; missing -> `missing_parameters`; redirect to `/account/connections?oauth_error=...` (l.16-21).
- `/_default/integrations/github/pr-review-oauth/` — F/pr-review-oauth-Csx51DC-.js:8-27. Search: `owner, repo, pr_page_path, require_app_installation`. Calls `ot`(app-initial-CMo_DJKv) `{returnTo: pr_page_path, owner, repo, requireAppInstallation}` -> `window.location.assign(url)`; on error navigate to pr_page_path.

## Toast hook `useOAuthCallbackToast` (PROVEN) I/useOAuthCallbackToast-Dy6KlUA6.js:5-49
- Reads `oauth_success` / `oauth_error` from location; success -> `settings.mcpLinkedSuccessfully`; error code map (14 codes) -> `settings.oauthFailed` "OAuth failed: {{message}}"; deletes `oauth_success`, `oauth_error`, `oauth_error_description` and replaceState.
- Copy (EN:20017): oauthMissingParameters "Missing OAuth parameters"; oauthExpiredState "OAuth session expired, please try again"; oauthTokenExchangeError "Failed to exchange authorization code"; oauthSaveTokensError "Failed to save OAuth tokens"; oauthAccessDenied "Access denied by the provider"; oauthConfigurationChanged "The MCP OAuth configuration changed. Start OAuth again."; oauthRouteChanged "The MCP OAuth network route changed. Start OAuth again."; oauthInstallationNotFound "The MCP server installation no longer exists."; oauthTokenExchangeFailed "Token exchange failed"; oauthMissingAccessToken "Token exchange succeeded but no access token was returned."; oauthCompleteError "Could not complete OAuth. Please try again."; oauthInsufficientPermissions "You don't have permission to manage MCP servers..."; oauthWrongUser "This OAuth link was created by a different user. Start the connection again from your own account."; oauthInsufficientScope "The provider did not grant the required permissions..."; oauthGenericError "Something went wrong, please try again"; oauthStartFailed "Unable to start OAuth link..."; oauthUnsafeAuthorizationUrl "The provider returned an invalid or unsupported OAuth sign-in URL."
- disconnect copy: disconnectIntegration "Disconnect {{integration}}"; disconnectDescription "Are you sure you want to disconnect your {{integration}} integration? This will remove all {{integration}} functionality from Devin."; connectedAccountsTitle "Connected accounts"

## User-level identity registry (PROVEN) F/app-initial-DGau-T9Q.js:487-563 (`z` table, providers `ir=['github','gitlab',(flag)'azure_devops']`)
- github: cloudStatus GET `integrations/github/user` -> {is_github_oauth_connected, github_username}; startCloudOauth GET `integrations/github/start-user-oauth?return_to=` -> {url}; deleteCloudIdentity DELETE `integrations/github/user`; cloudDependentQueryKeys [['user-profile',userId]]; hosts: GET `integrations/github/ghes/user-oauth-hosts` -> [{github_app_config_id, ghes_host, is_connected, username}]; startHostOauth GET `integrations/github/ghes/start-user-oauth?github_app_config_id=&return_to=`; deleteHostIdentity DELETE `integrations/github/ghes/user-oauth?github_app_config_id=` (l.243-292)
- gitlab: cloudStatus GET `integrations/git/user-oauth/gitlab/status` -> {is_connected, username}; start GET `integrations/git/user-oauth/gitlab/start?return_to=[&host=&app_config_id=]` -> {url}; delete DELETE `integrations/git/user-oauth/gitlab[?host=&app_config_id=]`; hosts GET `integrations/gitlab/self-hosted/user-oauth-hosts` -> [{gitlab_app_config_id, host, is_connected, username}] (l.449-468, 519-544)
- azure_devops: same generic `integrations/git/user-oauth/azure_devops/{status,start}`; status adds `can_connect`; hosts: null (l.545-561)
- Hook `or` (l.575-635): connect -> `window.location.assign(url)`, return_to = current pathname+search. disconnect -> delete then invalidate cloudStatus key + cloudDependentQueryKeys (or hosts key); toasts (l.643-650): "GHES account unlinked successfully"/"Failed to unlink GHES account"/"Failed to start GitHub Enterprise Server OAuth flow"/"GitLab account unlinked successfully"/"Failed to unlink GitLab account"/"Failed to start GitLab OAuth flow"; cloud kind -> no toast (ar() returns null l.564-566).
- Row shape (l.688-700): {key, provider, kind:'cloud'|'host', host, appConfigId, isConnected, username, canConnect, isReady}
- PR review OAuth: F/app-initial-CMo_DJKv.js:517-528 GET `integrations/github/pr-review-start-oauth?return_to=&pr_review_owner=&pr_review_repo=&require_app_installation=` -> {url}; `window.location.assign`; unauthenticated -> Auth0 `loginWithRedirectAndLog({authorizationParams:{connection:'github'}, appState:{returnTo:'/integrations/github/pr-review-oauth/?owner&repo&pr_page_path[&require_app_installation=true]'}})` (l.529-556). GET `pr-review/user-review-connections` -> {connections[]} (l.473-478).
- Route `/_user/settings/connected-accounts/` = redirect to `/settings/connections` (keeps `tab`) I/org._orgName.settings.connected-accounts.index-BJN9a9oJ.js:6-13
- `/_user/connect-git/` (F/connect-git-Cm249C0N.js): PLG teammate-invite landing; uses `w` (F/app-initial-BamWsN5Q.js:1538-1591) {hasOrgIntegration (connections.some(type!=='github_individual_token')), isGithubOAuthConnected, from}; copy "Connect your Git provider" / "You're all set" / "Try Devin now" / "Manage Git connections". GitHub org install: GET `${org}/integrations/github/installation-url?return_to=&auto_create_org=&_bypass_org_selection=` (DGau l.223-226)

## GitHub App (org) install + disconnect (PROVEN) F/app-initial-DGau-T9Q.js
- `vr.handleInstallation({newTab, autoCreateOrg, returnTo, onBeforeRedirect})` l.783-805: if newTab, pre-opens `window.open('about:blank','_blank')` then sets popup `location.href = url` (popup-blocker workaround); else `window.location.assign(url)`; on error closes popup. URL from GET `${org}/integrations/github/installation-url?return_to=&auto_create_org=&_bypass_org_selection=` (l.223-226).
- `Cr` installations modal hook l.885-988: `select_org` search param -> opens installations modal and strips param (l.894-900); `handleConnectInstallation(installation_id)` -> POST `${org}/integrations/github/connect-existing-installation` {installation_id} (l.253-255) then invalidates [`github-integration-status`,org],[`repos`,org,`github`],[`git-repos`],[`github-installations`],[`git-connections-metadata`,org],[`git-connection-my-access`,org],[`invite-git-manager-availability`,org],[C] (l.908-917); then sets `show_github_settings=true` in URL (l.929-934). `manageInstallation` -> GET `integrations/github/management-url?installation_id=&gh_org=&host=` -> `window.open(url,'_blank')` in setTimeout (l.937-955); 404 -> "We no longer have access to this installation. Try disconnecting and reconnecting." else "Error managing installation.". `onInstallNew({autoCreateOrg})` -> installation-url with return_to = current path+search -> `window.location.assign` (l.958-972).
- `yr.handleDelete` l.807-838: DELETE `${org}/integrations/github?name=&host=` (l.231-234) or DELETE `${org}/integrations/github/pat?connection_id=` (l.235-238); toast loading "Removing GitHub integration…" -> success "GitHub integration removed" / error "Failed to remove GitHub integration"; refetch [`github-integration-status`] + invalidates repos/git-repos/github-installations/git-connection-my-access/git-connections-metadata/invite-git-manager-availability.
- `br.handleConnectGithubUser` l.848-860: GET `integrations/github/start-user-oauth?return_to=<path>?<search>` -> assign. `Sr.handleAutoOrgCreationInstallation` l.871-880: GET `integrations/github/auto-org-creation-installation-url?return_to=` -> newTab ? window.open : assign.
- github-BiwNk-eu.js:1565-1573 GHES user OAuth start via `lt` (= mn) -> assign; l.593-602 GHES app installation-url -> `window.location.href`; l.604-617 delete GHES app config -> "Removed {app_name} ({ghes_host})", invalidates [`ghes-user-oauth-hosts`],[`ghes-app-configs`].

## Generic user-link hook `Lo(source,{openInNewTab})` (PROVEN) F/app-initial-BhLdCEaF.js:1306-1410
- Sources Ro = slack, github, jira, linear, microsoft-teams, pylon, incident-io, pagerduty (l.1431-1440); connectOrg only slack (zo, l.1441).
- linkUser: builds return path = current URL + `link_return=<source>` (So/xo l.1193, Do=`link_return` l.1221); linear adds `tool=linear` (l.1334). Per-source URL fn: slack ba(org,ret).authorize_url; github aa(ret).url (= start-user-oauth); linear h(org,ret).url; jira ia(org,ret).url (= `${org}/integrations/jira/user-link-url?return_to=` DGau l.299); teams u(org,ret).url; pylon da(org,ret).url; incident-io oa(); pagerduty c() (l.1337-1344). openInNewTab -> pre-open `about:blank` popup, `opener=null`, set popup.location.href; else `window.location.assign` (l.1325-1329,1349).
- Errors (l.1350-1373): default "Failed to link {source} user. Please try again."; 400 SLACK_NOT_CONNECTED -> "No Slack connection found for this organization. Please connect Slack to your organization first."; TEAMS_NOT_CONNECTED -> "Microsoft Teams is not connected for this organization..."; 403 teams -> "You don't have permission to access this organization...".
- connectOrg(slack, 'workspace'|'organization') l.1375-1405: param `connect_return=slack` (Oo l.1221) + `chain_user_signin=1` (Bo l.1442); ha/pa -> authorize_url; 403 -> "You don't have permission to connect {source} for this organization. Please ask an administrator."
- Return handling `Mo` l.1237-1300: reads `link_return`/`connect_return` once (wo l.1199-1210, strips via replaceState); jira `error` query: jira_site_access|atlassian_email_unverified|atlassian_account_in_use|atlassian_link_busy -> settings.atlassian* toasts (l.1249-1268); then polls Ao() up to Po=3 times every Fo=1500ms (l.1303) invalidating [`user-profile`],[`user-integrations`],[`member-integrations`],[`user-github-integration`],[`linear-user-access`],[sa],[o],(+[`slack`] on slack connect) (l.1222-1233); github link waits for user-profile.github_username else "GitHub link is taking longer than expected. Refresh the page to check its status." (l.1283-1289).
- Jira: unlink DELETE `${org}/integrations/jira/user-link` (DGau l.305); availability GET `.../jira/user-link-availability` (l.302). Jira agent: POST `integrations/jira-agent/link-info` {token}, POST `integrations/jira-agent/link`, POST `${org}/integrations/jira-agent/installations/unlink` (l.1299-1318) -- token-based, not OAuth.

## Status: DELIVERABLE WRITTEN (decompile-3.10.31/integrations/B-oauth-and-connection-lifecycle.md, ~268 lines, 10 sections). Late additions:
- Slack org connect endpoints: GET `${org}/integrations/slack/authorize?return_to=` / `admin-authorize` -> authorize_url (F/app-initial-BRqEELEv.js:272-279)
- auth1-oauth = first-party login + CLI PKCE (`/auth/cli/continue`), outposts.connect = create-outpost form; both excluded as non-integration.
- ManageConnectionAccessModal: repo-cache + git-permissions access editor (GIT_CONNECTION_VIEW/CONFIGURE/manage), copy at EN:21125-21150.
