# NOTES A — controller/transport (voice)
- dist-D8RCdqlE.js = @ricky0123/vad-web (SileroLegacy/SileroV5 at :14893/:14942, positiveSpeechThreshold :46, redemptionFrames :76, MicVAD getUserMedia :17958, AudioWorklet 'vad-helper-worklet' :17993, targetSampleRate 16000 :18016) + onnxruntime-web (ort-wasm-simd-threaded.jsep.mjs :1295). NOT a realtime transport SDK.
- build-BrM3pv_h.js = ACP client/session store (protocolVersion 2 :6232) — task B territory.
## VoiceCallController-D1fhynnv.js (formatted, 995 lines)
- Constants :862-891: P=20000 (ms), F=8, I=500, L=5000, R=Set([1001,1006,1012]) close codes, z='Voice call has ended', B maps server error strings->i18n keys (Too many calls/already active/not enabled), V='/assets/vad/0.0.30-1.27.0/' (vad-web 0.0.30 + ort 1.27.0), H=0.5 pos thresh, U=0.35 neg thresh, W=500 redemptionMs, G=250 minSpeechMs, K=-50 dB floor, q=50ms analyser interval.
- URL builder k() :534-538: `${apiBase()}/voice/${devinId}/live?token=${accessToken}&org_id=${orgId}[&<C>=<T()>]` (C/T from app-initial-CMo_DJKv — resolve).
- MicVAD setup ae() :455-513: model v5, thresholds above, volume via RMS dB (O() :447) -> store.setVolume(0-100); fallback analyser oe() :514-529 (50ms interval).
- Push-to-talk X() :895-940: keybind y.SessionPushToTalk (Space), only when status==='live' && muted; startTalking/stopTalking on controller; releases on keyup/blur/hidden.
- Z=VoiceCallController :941-978: hangs up on orgId change; error toast with Retry (when status idle && devinId).
- Store h (zustand) from app-initial-ClK3cO3o `zt`: fields status(idle|error|live|...), muted, deafened, controller, devinId, error, volume.
- TRANSPORT (PROVEN): WebRTC (RTCPeerConnection :722, addTrack mic, ontrack -> <audio autoplay>) with WebSocket signaling to `${apiBase}/voice/{devinId}/live?token=&org_id=` (:534). Client sends JSON: {type:'offer',sdp} :764, {type:'resume',live_session_id} :690, {type:'end',live_session_id} :592 (fire-and-forget on fresh WS when main WS not open), {type:'ping'} every 20s :634. Server->client: {type:'answer',sdp,live_session_id,voice_session_id} :641; {type:'resumed'} :645; {type:'transcript',turn_id,role,text,started_at?,done?,event_id?} :648-653; {type:'error'} :655 -> voiceCall.voiceModelError. No ICE trickle: waits icegatheringstatechange complete or 2s timeout :746-756, then sends full SDP (vanilla ICE).
- Reconnect :665-702: on ws close, if live_session_id set && pc connected && code!=1000 && reason!='Voice call has ended' && (already retrying || code in {1001,1006,1012}) && attempts<8 -> status 'reconnecting', backoff min(500*2^n,5000), send resume. >=8 -> error voiceCall.connectionClosed {code}. Close code 1000 or reason z => clean end (no error). Other reasons -> B map or raw reason.
- startCall G() :773-811: refuses if status in connecting/live/reconnecting; opts {mic?:MediaStream, ready?:Promise} (pre-acquired mic from button); sets state devinId,sessionTitle:null,status:'connecting',...; W() :711 plays 'dial', ringStartedAtMs, getUserMedia({audio:true}), tracks disabled until unmuted (muted=false at start so D true → enabled), pc.onconnectionstatechange connected → 'live', connectedAtMs, sound 'join'; failed/closed → hangUp.
- hangUp V() :595-621: sound 'leave' if joined, sends end, tears everything down, store.reset(), sets ended:{devinId,voiceSessionId}.
- toggleMute/toggleDeafen sounds mute/unmute/deafen/undeafen :813-820; PTT startTalking/stopTalking sounds ptt-start/ptt-stop :821-826 (startTalking sets muted:false, talking:true; stop sets muted:true).
- mic track.enabled = (live||reconnecting) && !muted :571-578; audio el.muted = deafened :579-586.
- Sounds gated by user preferences.notification_sound_enabled :566-570.
- ie(devinId, hangUp) from voiceCallChannel :856 — cross-tab channel.
## voiceCallChannel-Aup4TwTd.js (33 lines): BroadcastChannel `voice-call:${devinId}`. msgs: who-holds / holding / hang-up. r()=holder (controller): replies holding, hangs up on hang-up. i()=observer (presence): asks who-holds, callback on holding. n()=postHangUp.
## VoiceCallPresence-BHY6sHdG.js (55 lines): reads globalState activeVoiceCall{userId, voiceSessionId} + devinData.devin_id; shows avatar + 'voiceCall.presence.you'|otherUser{name}|someone; 'End call' button only when this tab (some tab of mine) holds the call (via channel). Single-caller model (one activeVoiceCall per session).
## voiceCallSounds-BmJYkLKX.js (188 lines): NO audio assets. All tones synthesized via WebAudio (createBuffer additive synth w/ harmonics, exp decay, tanh). Notes C3..C5 (130.81..523.25). c(kind) :86-127: dial (C4 blip + C5 tick, then starts looping ring o() after k=0.9s), join (G3-C4-E4 arpeggio), leave (E4-C4-G3), mute/ptt-stop (A3 0.1s), unmute/ptt-start (E4 0.1s), deafen (C4 then G3), undeafen (G3 then C4). Ring loop o() :50-79: 20 steps x 110ms melody [C4,E4,G4,G4,A4,G4,E4] over bass [C3,E3,G3,C4,G3,E3]; E export = ring rhythm meta {stepMs:110, loopSteps:20, beats:[{step0,w1},{step6,w0.85}]} (used by UI ring animation); a()=ringStartedAt (Date.now()+delay), i()=stopRing.
## Entry: InputBox-CFvzp3_F.js :19285-19314 mo(): if devinId(h) exists && !ti → store.startCall(devinId). Else (new session): getUserMedia first (error toast voiceCall.microphoneUnavailable), then Ta({voiceCall:(devinId, readyPromise)=>startCall(devinId,{mic,ready})}) — session creation = task B. ho() = guarded submit → mo(); go() = hangUp.
## VoiceCallButton-Dihi99Wb.js: exports VoiceCallButton(Y)=memo(be) + SidebarVoiceCall(Q)=memo(xe), both gated by c()=`So` from build-BrM3pv_h.js:1880?? (check). be() :318 renders ONLY when status in live/connecting/reconnecting for current session → in-call bar (waveform, duration, mute, deafen, hangup). xe() :400 = floating sidebar pill w/ 'returnToCall' link + transcript popover (ye :260-300: voiceCall.transcript/transcriptEmpty/transcriptYou/transcriptDevin).
## composer-voice-FeMz50MG.js: presentational only. M=waveform w/ ring rhythm; N=recording bar (onStop/processing) = dictation (not call). Default ringRhythm J {stepMs:1100}.
## GATE (PROVEN): InputBox qr = Ft(app-initial-ClK3cO3o:3668)=Tu()=We() = build-BrM3pv_h.js Sc :2787 = useFeatureFlag('devin-voice-mode') via I() :2487 (reads feature-flags query L(), telemetry addFeatureFlagEvaluation). Eu() :2759 = flag && controller!==null. Store Cu (zt) :2694-2757: fields devinId,sessionTitle,status idle|connecting|live|reconnecting|error,error,connectedAtMs,ringStartedAtMs,muted,deafened,talking,volume,turns,transcriptTurns,ended{devinId,voiceSessionId},controller; turnId prefix 'live-voice-turn-' :2692. finishTurn w/ eventId keeps turn (eventId), w/o removes (task B boundary).
## Bootstrap (PROVEN): NOT a token/room. Create-session request (requests-DN_jEueP.js:168,193) sends `voice_call_bootstrap: true` boolean when composer starts a new session via call button (InputBox:19062 {voiceCall, voiceCallBootstrap}; useInputBox-U7TgCrww:1166,1290). ACP meta schema (en-CsbykFSh.js:5126) "cognition.ai/voiceCallBootstrap": boolean nullish on message meta; (:5360) "cognition.ai/voiceCallEnabled": bool, "cognition.ai/userId", "cognition.ai/voiceSessionId" on session meta; Np() :8603 = voiceCallEnabled===true. All real credentials = access token + org_id in WS query string; SDP answer from server. Vendor of realtime backend = REMOTE/UNKNOWN.
## Desktop shell (PROVEN): extension.js:5492 meta "cognition.ai/voiceChannel": string; :6822 voiceCallBootstrap bool; :7046-7048 voiceCallEnabled/userId/voiceSessionId; :11154 isVoiceCallStart(meta)=voiceCallEnabled===true; :11157 getSetVoiceModeMeta; :11164 getVoiceCallDurationSeconds = meta "cognition.ai/durationSeconds". sessions.desktop.main.js: event types 'voice_call_started'/'voice_call_ended' :782090,:801954-801958 (derived from voiceCallEnabled meta & durationSeconds); :802563 voiceCallWindows (transcript grouping); :360833 voiceBootstrapSenderId set when voiceCallBootstrap meta && sender. Transport code (RTCPeerConnection/WS /voice/) is web-only — desktop shell only renders events. (Task B: turns→messages.)
- DELIVERABLE WRITTEN: decompile-3.10.31/voice/A-controller-transport-signaling.md (complete)
